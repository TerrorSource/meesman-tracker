services:
  meesman-tracker:
    # Let op: registry-namen moeten lowercase zijn
    image: ghcr.io/terrorsource/meesman-tracker:latest
    container_name: meesman-tracker
    ports:
      - "8080:8080"
    environment:
      - TZ=Europe/Amsterdam
      - DB_PATH=/data/app.db
      - CONFIG_PATH=/data/config.yaml
      # ── Beveiliging (v10, aanbevolen — zie README) ──
      # - APP_USER=marc                      # Basic Auth voor alle pagina's
      # - APP_PASSWORD=kies-een-sterk-wachtwoord
      # - API_TOKEN=lange-willekeurige-string # voor Home Assistant (/api/*, exports)
      # - MASTER_KEY=...                     # waarde uit data/config.yaml (optioneel)
      # ── Non-root: uid/gid van de eigenaar van de data-map (zie `id`) ──
      # - PUID=1000
      # - PGID=1000
    volumes:
      - ./data:/data
    restart: unless-stopped
    # Mini-init als PID 1 (zombie-processen opruimen). Het image heeft tini
    # al ingebouwd; dit is een extra vangnet.
    init: true
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://localhost:8080/health"]
      interval: 60s
      timeout: 10s
      retries: 3
      start_period: 30s
