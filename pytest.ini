[pytest]
# Repo-root op sys.path zodat `import app` werkt, ook bij een kale `pytest`
# (zonder `python -m`), zoals in GitHub Actions.
pythonpath = .
testpaths = tests
filterwarnings =
    ignore::DeprecationWarning
